# Copy to .env and fill in — or better: set these via the app Profile page
# (stored encrypted in the SQLite vault). .env is optional and only used as
# a fallback when the vault is empty. NEVER commit .env.
#
# CAT8000_USERNAME=reqasse
# CAT8000_PASSWORD=change-me
# CAT8000_SECRET=change-me
